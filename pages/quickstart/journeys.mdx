import MainLayout from "../../components/layout/MainLayout";

# Journeys

Let's get started

<Box css={{
    width:'100%',
    justifyContent: 'space-between',
    flexDirection: 'row',
    marginTop: '$6'
}}>
<StyledLink href={'/overview'}><Button color={ButtonColor.secondary} size={ButtonSize.large}> ← Home </Button></StyledLink>
<StyledLink href={'/quickstart/first-api'}><Button size={ButtonSize.large}> First API Call → </Button></StyledLink>
</Box>

export default ({ children }) => <MainLayout>{children}</MainLayout>;
