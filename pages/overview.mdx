import MainLayout from '../components/layout/MainLayout';
import {Button} from '../components/Button/Button';

# Overview


Alloy SDK is a library used to render client-side components that interact with Alloy's identity-decisioning and compliance platform. It can handle collecting and verifying of documents, phone number, and more. 

<div style={{marginTop:'12px'}}>
<Button> Get Started </Button> 
<Button color={"secondary"} > Try Demo </Button>
</div>

export default ({ children }) => <MainLayout>{children}</MainLayout>
